@model IEnumerable<churchAttendace.Models.Entities.Session>
@{
    ViewData["Title"] = "تسجيل الحضور";
    ViewData["PageTitle"] = "تسجيل الحضور";
    ViewData["PageSubtitle"] = "اختيار جلسة لتسجيل حضور الأطفال";
    ViewData["Breadcrumbs"] = new List<BreadcrumbItem>
    {
        new() { Title = "لوحة التحكم", Url = Url.Action("Index", "Dashboard", new { area = "Admin" }) },
        new() { Title = "تسجيل الحضور", IsActive = true }
    };
}

<div class="app-card animate__animated animate__fadeInUp mb-4">
    <div class="app-card-header">
        <div>
            <h2 class="h6 mb-1">فلترة حسب الفصل</h2>
            <div class="text-muted small">اختر الفصل المناسب لعرض الجلسات</div>
        </div>
    </div>
    <form method="get" class="row g-2 align-items-end">
        <div class="col-12 col-md-6 col-lg-4">
            <label class="form-label">الفصل</label>
            <select name="classId" class="form-select">
                <option value="">كل الفصول</option>
                @if (ViewData["Classes"] is SelectList classes)
                {
                    foreach (var option in classes)
                    {
                        <option value="@option.Value" selected="@(option.Value == (ViewData["ClassId"]?.ToString() ?? string.Empty))">@option.Text</option>
                    }
                }
            </select>
        </div>
        <div class="col-12">
            <button type="submit" class="btn btn-outline-primary app-btn">
                <i class="bi bi-filter"></i>
                تصفية
            </button>
        </div>
    </form>
</div>

<div class="app-card animate__animated animate__fadeInUp">
    <div class="table-responsive">
        <table class="table app-table align-middle">
            <thead>
                <tr>
                    <th>الجلسة</th>
                    <th>الفصل</th>
                    <th>التاريخ</th>
                    <th class="text-end">الإجراء</th>
                </tr>
            </thead>
            <tbody>
                @if (!Model.Any())
                {
                    <tr>
                        <td colspan="4" class="text-center text-muted">لا توجد جلسات لتسجيل الحضور.</td>
                    </tr>
                }
                else
                {
                    foreach (var session in Model)
                    {
                        <tr>
                            <td>@session.SessionName</td>
                            <td>@session.Class?.Name</td>
                            <td>@session.SessionDate.ToString("yyyy/MM/dd")</td>
                            <td class="text-end">
                                <a class="btn btn-primary app-btn btn-sm" asp-action="Take" asp-route-id="@session.Id">
                                    <i class="bi bi-clipboard-check"></i>
                                    تسجيل
                                </a>
                            </td>
                        </tr>
                    }
                }
            </tbody>
        </table>
    </div>
</div>
